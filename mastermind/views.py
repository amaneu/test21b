from django.shortcuts import render
from django.http import JsonResponse

def create_game(request):
	return JsonResponse({})

def detail(request):
	return JsonResponse({})

def guess(request):
	return JsonResponse({})
